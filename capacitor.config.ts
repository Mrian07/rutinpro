import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.rutinpro.app",
  appName: "RutinPro",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
